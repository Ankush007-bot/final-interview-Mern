// 🔥 Top HTML5 Features

// Semantic Tags
// New meaningful tags such as <header>, <footer>, <section>, <article>, <nav> improve readability and SEO.

// Audio & Video Support
// Play multimedia without plugins:

// <audio controls></audio>
// <video controls></video>


// Canvas & SVG
// Used to draw graphics, charts, animations inside the browser.

// Local Storage & Session Storage
// Store data in browser without cookies.

// localStorage → permanent until cleared

// sessionStorage → clears when browser closes

// Geolocation API
// Detect user's location with user permission.

// Form Enhancements
// New input types and attributes like:
// email, date, number, range, required, placeholder, etc.

// Offline Web Applications (Cache / Service Workers)
// Apps can work without internet connection.

// WebSockets
// Real-time communication between client and server (like chat apps).

// Drag and Drop API
// Allows dragging & dropping elements inside webpage.

// Responsive Design with Viewport
// <meta name="viewport" content="width=device-width, initial-scale=1.0">
// Makes websites mobile-friendly.